<%= simple_form_for(@cargo) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :nomcar %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
